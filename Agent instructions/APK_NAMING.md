# APK naming

Always include the version number in the APK filename when building for testing.

- `thecube-<package.json version>-debug.apk`
- Optional: `thecube-<version>-YYYYMMDD-HHmmss.apk`

Do not use versionless names such as `thecube-debug.apk`.
